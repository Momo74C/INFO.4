#include <stdio.h>

struct nodo {
    int info;
    next* nodo;
}

int main()
{
    nodo 
}



void ins(nodo  *s);
{
    nodo *p;
    p = new nodo;
    p->info=5;
    p->next=s;
    s=p;
}
int can(nodo *s);
